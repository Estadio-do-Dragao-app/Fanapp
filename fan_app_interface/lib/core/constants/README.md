Constants (strings, keys, endpoints)

This folder should define application-wide constants such as API endpoints,
feature flags, hard-coded asset names and local storage keys.

Prefer grouping constants by domain (e.g., `api.dart`, `storage_keys.dart`).
