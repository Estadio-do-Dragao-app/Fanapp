Helpers and utility functions

Small, pure helper functions that are used across the app. Keep these tiny and
well-tested. Avoid UI code here.

Examples:
- `location_utils.dart` - coordinate helpers
- `string_utils.dart` - common string formatting helpers
