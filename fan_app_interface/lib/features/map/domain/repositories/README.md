Repository contracts for Map feature

Place interface files here, e.g. `map_repository.dart` describing methods like
`Future<List<Poi>> getPois()` and caching/refresh semantics.

Keep domain repository interfaces free from platform details.
